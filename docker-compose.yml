services:
  api-tests:
    build: .
    container_name: api_testing_restassured
    volumes:
      # Mount the target folder to access test results (including Allure reports) locally
      - ./target:/app/target
      # Cache Maven dependencies between runs to avoid re-downloading every time
      - maven-repo:/root/.m2

volumes:
  maven-repo:
